import React from "react";
import Navigation from "@/Components/Navigation";

const NavBar = () => {
    return (
        <nav className="">
            <Navigation />
        </nav>
    );
};

export default NavBar;
